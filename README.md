# Django Stripe Store

Django веб-приложение для интернет-магазина с интеграцией платежей через Stripe. Проект поддерживает продажу товаров и заказов с возможностью применения скидок и налогов.

## 🚀 Возможности

- **Управление товарами**: Создание и отображение товаров с ценами в центах
- **Много валютная поддержка**: USD и EUR
- **Заказы**: Группировка товаров в заказы с применением скидок и налогов
- **Интеграция Stripe**: Безопасная обработка платежей через Payment Intents
- **Современный UI**: Tailwind CSS для красивого интерфейса
- **Docker поддержка**: Легкое развертывание в контейнерах

## 🛠 Технологии

- **Backend**: Django 6.0
- **Платежи**: Stripe API
- **База данных**: SQLite
- **Фронтенд**: HTML, Tailwind CSS, JavaScript
- **Контейнеризация**: Docker & Docker Compose

## 📋 Требования

- Python 3.12+
- Docker & Docker Compose (для контейнерного запуска)
- Аккаунт Stripe с API ключами

## ⚙️ Установка и настройка

### 1. Клонирование репозитория

```bash
git clone https://github.com/isHardCoded/django-stripe-test
cd django-stripe-test
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Django настройки
DJANGO_SECRET_KEY=your-django-secret-key-here
DEBUG=1

# Stripe API ключи для USD
STRIPE_PUBLIC_KEY_USD=pk_test_your_public_key_usd
STRIPE_SECRET_KEY_USD=sk_test_your_secret_key_usd

# Stripe API ключи для EUR
STRIPE_PUBLIC_KEY_EUR=pk_test_your_public_key_eur
STRIPE_SECRET_KEY_EUR=sk_test_your_secret_key_eur
```

> **Важно**: Получите API ключи в [Stripe Dashboard](https://dashboard.stripe.com/)

### 3. Запуск с Docker

```bash
# Сборка и запуск контейнера
docker-compose up --build

# Или в фоне
docker-compose up -d --build
```

Приложение будет доступно по адресу: http://localhost:8000

### 4. Локальный запуск

```bash
# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt

# Миграции базы данных
python manage.py migrate

# Запуск сервера разработки
python manage.py runserver
```

## 📊 Структура проекта

```
django-stripe-test/
├── config/                 # Настройки Django
│   ├── settings.py        # Основные настройки
│   └── urls.py            # URL маршруты
├── store/                 # Основное приложение
│   ├── models.py          # Модели данных (Item, Order, Discount, Tax)
│   ├── views.py           # Представления и логика оплаты
│   ├── stripe_client.py   # Инициализация Stripe клиента
│   ├── templates/         # HTML шаблоны
│   └── urls.py            # Маршруты приложения
├── Dockerfile            # Docker образ
├── docker-compose.yml    # Docker Compose конфигурация
├── requirements.txt      # Python зависимости
└── manage.py            # Django management script
```

## 🗄 Модели данных

### Item (Товар)
- `name`: Название товара
- `description`: Описание
- `price`: Цена в центах (целое число)
- `currency`: Валюта (usd/eur)

### Order (Заказ)
- `items`: Связанные товары (ManyToMany)
- `discounts`: Примененные скидки (ManyToMany)
- `taxes`: Примененные налоги (ManyToMany)
- `created_at`: Дата создания

### Discount (Скидка)
- `name`: Название скидки
- `amount`: Сумма скидки в центах

### Tax (Налог)
- `name`: Название налога
- `percentage`: Процент налога (float)

## 🌐 API Endpoints

### Товары
- `GET /items/` - Список всех товаров
- `GET /item/<id>/` - Детальная страница товара с формой оплаты
- `POST /buy_item/<id>/` - Создание Payment Intent для товара

### Заказы
- `GET /orders/` - Список всех заказов
- `GET /order/<id>/` - Детальная страница заказа с формой оплаты
- `POST /buy_order/<id>/` - Создание Payment Intent для заказа

## 💳 Работа с платежами

### Оплата товара
1. Перейдите на страницу товара `/item/<id>/`
2. Введите данные карты
3. Подтвердите оплату

### Оплата заказа
1. Перейдите на страницу заказа `/order/<id>/`
2. Система автоматически рассчитает итоговую сумму с учетом скидок и налогов
3. Введите данные карты
4. Подтвердите оплату

## 🔧 Администрирование

Доступ к Django Admin: http://localhost:8000/admin/

Создайте суперпользователя:
```bash
docker-compose exec web python manage.py createsuperuser
# или локально
python manage.py createsuperuser
```

## 🧪 Тестирование платежей

Для тестирования используйте тестовые карты Stripe:
- **Успешная оплата**: `4242 4242 4242 4242`
- **Отклоненная оплата**: `4000 0000 0000 0002`

Любой будущий expiry date и любой CVC код подойдут.
